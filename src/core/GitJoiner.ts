// Stub: reserves the seam for v2 ROI work (git log × events join).
// v1 ships with the `git_commits` table already created (see EventStore migration)
// and this no-op export. v2 fills in the body.
export function noop(): void {
  /* intentional */
}
